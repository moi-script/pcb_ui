# Core pipeline
matplotlib

# Web API (server.py) + MongoDB
fastapi
uvicorn[standard]
python-multipart
pymongo

# Serial streaming: the machine session (machine.py, grbl/) and the
# standalone pcb_send.py CLI. TRACEWORKS_SIM=1 needs no hardware.
pyserial

# Image tracing (tracer/) — centerline skeletonisation
numpy
pillow
scikit-image
opencv-python-headless

# Tests
pytest
